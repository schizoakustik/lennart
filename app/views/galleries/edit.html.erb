<div class="row">
  <div class="small-8 large-4 columns panel">
    <h4>Ändra album</h4>
  </div>
</div>
<div id="maximage">
  <% @pictures.each do |picture| %>
    <%= image_tag (picture.image) %>
  <% end %>
</div>

<%= render 'thumb_menu' %>
<%= render 'form' %>

<script type="text/javascript" charset="utf-8">
  
  
$(function() {
    $('#maximage').maximage({
        backgroundSize: "contain",
        cycleOptions: {
            fx: 'fadeZoom',
            //sync: false,
            speed: 800,
            timeout: 5000,
            pause: 1
        },
        onFirstImageLoaded: function() {
            jQuery('#maximage').fadeIn('fast');
          }
      
    });
});
</script>
